[package]
name = "email-sender-smtp"
version = "0.1.0"
edition = "2024"
description = "SMTP email sender implementation using lettre for backend-kit."

[dependencies]
async-trait = "0.1"
email-sender = { path = "../email-sender" }
lettre = { version = "0.11", default-features = false, features = ["builder", "smtp-transport", "tokio1-rustls-tls"] }

[dev-dependencies]

[lints]
workspace = true
