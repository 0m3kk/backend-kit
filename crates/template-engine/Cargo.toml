[package]
name = "template-engine"
version = "0.1.0"
edition = "2024"
description = "Core traits, context builder, and error definitions for generic template rendering in backend-kit."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[lints]
workspace = true
