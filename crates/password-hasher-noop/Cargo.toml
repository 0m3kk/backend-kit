[package]
name = "password-hasher-noop"
version = "0.1.0"
edition = "2024"
description = "No-op plaintext password hasher for testing (the password-hasher crate)."

[features]
default = ["async"]
async = ["dep:async-trait"]

[dependencies]
async-trait = { version = "0.1", optional = true }
password-hasher = { path = "../password-hasher" }

[lints]
workspace = true
